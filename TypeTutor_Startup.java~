
import javax.swing.*;

public class TypeTutor_Startup
{
  public static void main(String[] args) 
  {
    JFrame window = new JFrame();
    LoginModel model = new LoginModel(window);
    LoginView view = new LoginView(model, window);
  }
}